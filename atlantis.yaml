version: 3
projects:
  - dir: .
    name: stage
    workspace: stage
    autoplan:
      when_modified: ["*.tf"]
      enabled: true
  - dir: .
    name: prod
    workspace: prod
    autoplan:
      when_modified: ["*.tf"]
      enabled: true
